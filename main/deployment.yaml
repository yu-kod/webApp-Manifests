kind: Deployment
apiVersion: apps/v1
front:
  imageTag: "tasyuminahito/webapp-cicd:main"
metadata:
  name: webapp-deployment22
spec:
  replicas: 1
  selector:
    matchLabels:
      app: django
  template:
    metadata:
      labels:
        app: django
    spec:
      containers:
        - name: django
          image: "tasyuminahito/webapp-cicd:main"
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              name: django
